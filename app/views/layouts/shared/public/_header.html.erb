<nav class="navbar navbar-expand-md navbar-light bg-light box">
  <%= link_to 'Ruby -T', root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse box" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto box">
      <% if current_user %>
        <li class="nav-item dropdown">
          <%= link_to '#', class: 'nav-link dropdown-toggle', id: 'profileDropdown', data: { toggle: 'dropdown' }  do %>
            <%= "Bonjour #{current_user.email}" %>
          <% end %>
          <div class="dropdown-menu">
            <%= link_to 'Accéder au profil', edit_user_registration_path, class: 'dropdown-item' %>
            <div class="dropdown-divider"></div>
            <%= link_to 'Se déconnecter', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
          </div>
        </li>
        <% if current_user.admin %>
          <li class="nav-item">
            <%= link_to "Dashboard", administration_path, class: 'nav-link' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Mon Panier", cart_path, class: 'nav-link' %>
          </li>
        <% end %>
      <% else %>
        <li class="nav-item dropdown">
          <%= link_to '#', class: 'nav-link dropdown-toggle', id: 'loginDropdown', data: { toggle: 'dropdown' }  do %>
            Bonjour invité
          <% end %>
          <div class="dropdown-menu">
            <%= link_to 'Login', new_user_session_path, class: 'dropdown-item' %>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
